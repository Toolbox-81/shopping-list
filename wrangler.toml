# Cloudflare Pages CLI 배포용 설정 (선택 사항)
# 대시보드에서 Git 연동으로 배포한다면 이 파일은 없어도 무방합니다.
# CLI로 배포하고 싶다면:
#   npx wrangler pages deploy . --project-name=grocery-checklist

name = "grocery-checklist"
pages_build_output_dir = "."
compatibility_date = "2024-01-01"
