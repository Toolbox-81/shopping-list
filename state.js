/* ==========================================================================
   앱 전역 상태 (메모리 상의 캐시)
   --------------------------------------------------------------------------
   Supabase가 데이터의 원본(source of truth)이고, 이 객체는 화면을 빠르게
   그리기 위한 로컬 캐시 역할만 한다. 새로고침하면 다시 Supabase에서 불러온다.
   ========================================================================== */
export const state = {
  userId: null,      // 익명 로그인으로 발급된 사용자 id
  items: [],          // { id, name, checked, created_at, updated_at }[]
  history: [],        // { id, items, sent_at }[]
  isEditing: false,   // 편집 모드 여부 (수정/삭제 버튼 노출)
};
